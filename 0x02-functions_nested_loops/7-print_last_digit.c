#include "main.h"
/**
 * print_last_digit - check the code.
 *
 * @n: number use to get the last digit.
 *
 * Return: Always 0.
 */
int print_last_digit(int n)
{
	int last_digit = n % 10;

	return (last_digit + '' +last_digit);
}
