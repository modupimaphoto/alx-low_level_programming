#include "main.h"
#include <string.h>
/**
 * _strlen - eturns the length of a string.
 *
 * @s: string to check.
 *
 * Return: Always string length.
 */
int _strlen(char *s)
{
	int str_len = strlen(s);

	return (str_len);
}
