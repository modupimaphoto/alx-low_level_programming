#include "main.h"
#include <string.h>
/**
 * _strcmp - _strcmp
 * @s1: type string.
 * @s2: type string.
 * Return: results.
 */
int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);

	return (result);
}
